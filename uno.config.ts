import { defineConfig } from "unocss";

export default defineConfig({
  theme: {
    backgroundColor: {
      primary: "#ececec",
      "primary-hover": "#dcdcdc",
      "primary-active": "#ccc"
    }
  }
});
